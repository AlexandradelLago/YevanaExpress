<!-- alquiler/vans.ejs -->

<ul>
        <% items.forEach((item) => { %>
          <li>
            <%= item.brand %>  
            <%= item.description.short %>
          </li>
        <%  }) %>
      </ul>